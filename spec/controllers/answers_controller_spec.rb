# frozen_string_literal: true

require 'rails_helper'

RSpec.describe AnswersController do
  # let(:user)    { create(:user)    }
  # let(:answer)  { create(:answer)  }
  # let(:review)  { create(:review)  }
  # let(:product) { create(:product) }

  # before do
  #   sign_in(user)
  # end

  # describe 'POST #create' do
  #   it 'create new answer' do
  #     expect do
  #       post :create,
  #            params: { product_id: product.id, review_id: review.id, answer: attributes_for(:answer), product: }
  #     end.to change(Answer, :count).by(1)
  #   end
  # end

  # describe 'PUT #update' do
  #   it 'update answer' do
  #     expect do
  #       put :update, params: { id: answer.id, body: 'new body', product_id: product.id }
  #     end.not_to change(Answer, :count)
  #   end
  # end

  # describe 'DELETE #destroy' do
  #   it 'destroy answer' do
  #     expect do
  #       delete :destroy, params: { id: answer.id, product_id: product.id, review_id: review.id }
  #     end.to change(Answer, :count).by(-1)
  #   end
  # end
end
