<%= turbo_stream.replace 'comments', partial: 'comments/comments', locals: { product: @product } %>
<%= turbo_stream.replace Comment.new, partial: 'comments/new', locals: { product: @product, comment: Comment.new } %>
<%= turbo_stream.replace product_path(@product) %>