<%= turbo_stream.remove @review %>
<%= turbo_stream.replace 'reviews', partial: 'reviews/reviews', locals: { product: @product, reviews: @reviews } %>
<%= turbo_stream.replace Review.new, partial: 'reviews/new', locals: { product: @product, review: Review.new } %>